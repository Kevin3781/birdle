# Secrets — never commit the Xeno-canto key
scripts/.xeno_canto_key
scripts/secrets.env
.env
.env.local

# Local Claude Code overrides (personal, machine-specific)
.claude/settings.local.json

# Python
__pycache__/
*.pyc
.venv/

# Pipeline transients (regenerable; final outputs live in js/birds_ingested.js + js/media.js)
scripts/*.log
scripts/global_freeze_before_cleanup.txt
scripts/clues_batches/

# Test artifacts (regenerable — run test_birdle.py / capture_screens.py)
screenshots/
